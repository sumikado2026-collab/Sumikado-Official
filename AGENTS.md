# 澄花堂網站 Agent 強制規範

本檔案位於網站 Git repository 根目錄，適用於本目錄及所有子目錄。每個新對話、工作階段或子任務開始處理網站前，都必須先完整閱讀本檔案。這些規則是完成工作的必要條件，不得因修改範圍小、只改文字或先前已檢查過而略過。

## 開始工作前

1. 先閱讀本檔案，再執行任何網站搜尋、編輯或部署操作。
2. 執行 `git status --short`，辨識並保留使用者既有或不相關的修改。
3. 先檢查 `http://localhost:8080` 是否已有可用預覽；若無，才於本目錄啟動 `python3 -m http.server 8080`。
4. 修改前先查看相關頁面的目前呈現，確認修改基準與影響範圍。

## 修改與驗證

1. 將一組可一起檢查的修改視為一個完整里程碑；草稿與實驗內容維持在本機，不得混入部署。
2. 使用適合的最小範圍修改，並同步處理受影響的繁中、日文、英文內容、圖片替代文字與無障礙標示。
3. 修改完成後先執行：

   ```bash
   node scripts/validate-site.js
   ```

4. 驗證通過後，必須在 `http://localhost:8080` 重新載入並實際檢查受影響頁面；依修改內容涵蓋：
   - 桌面版與手機版
   - 繁中、日文與英文
   - 圖片、連結、按鈕與選單
   - 文字是否溢出、跑版，以及整體視覺一致性
5. 驗證未完成或失敗時，不得宣告工作完成或進行部署。

## 部署授權

- `main` 是測試預覽分支；推送後會自動部署至 Cloud Run 測試預覽環境。
- 不得推送草稿、實驗、未完成內容或與本里程碑無關的檔案。
- **不得部署、推送或同步到 `main`，除非使用者在目前要求中明確回覆「部署」或 `deploy`。**
- 使用者授權部署後，仍須先完成修改摘要、必要驗證及待提交檔案檢查，才可提交並推送。
- 推送後應確認遠端已收到 commit；若可取得部署狀態，也應確認部署結果。

## 最終回覆必要內容

只要本次工作變更了任何檔案、內容或設定，最終回覆必須包含：

1. 修改摘要。
2. 已完成的驗證與結果。
3. 同步狀態：
   - 若尚未部署，最終回覆的最後必須獨立保留：

     > **目前的改動尚未同步更新到網站，如果您需要同步請回答「部署」。**

   - 若已依使用者授權完成部署，明確說明已推送的分支與 commit，不得再聲稱「尚未同步」。
